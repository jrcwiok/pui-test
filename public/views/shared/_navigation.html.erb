<section id="navigation">
  <nav class="navbar navbar-default" aria-label="<%= I18n.t("navigation.top_level") %>">
    <div class="container-fluid navbar-header top-bar">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapsemenu"
              aria-expanded="false">
        <span class="sr-only"><%= I18n.t("navbar.toggle_navigation") %></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsemenu">
        <ul class="nav nav navbar-nav">
          <li class="dropdown">
            <a class="dropdown-toggle" href="<%= app_prefix("/repositories") %>">
              <%= t('repository._plural') %> <span class="caret hidden-xs"></span></a>
            <ul class="dropdown-menu hidden-xs">

<%#
need a real way to do this...
but for now, i'm just hardcoding this as a proof of concept
(before moving into PROD, move the CSS bit outta here, too.)
%>

<li><a href="<%= app_prefix("/repositories/1") %>">Arts Library</a></li>
<li><a href="<%= app_prefix("/repositories/2") %>">Beinecke Library</a></li>
</ul>
          <%# TODO: add class="active" if we're on that page %>
          <% $MAIN_MENU.each do |link| %>
            <li><a href="<%= app_prefix(link[0]) %>"><%= t(link[1]) %></a></li>
          <% end %>
          <% unless AppConfig[:pui_hide][:search_tab] %>
            <li><a href="<%=  app_prefix('/search?reset=true') %>" title="<%= I18n.t('search_tab', :target => t('archive._plural'))%>">
                <span class="fa fa-search" aria-hidden="true"></span>
                <span class="sr-only"><%= I18n.t('search_tab', :target => t('archive._plural')) %></span>
              </a>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</section>
